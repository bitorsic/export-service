# Database Credentials
DB_USER=postgres
DB_PASSWORD=postgrespassword
DB_NAME=export_service
DB_HOST=localhost
DB_PORT=5432

# Combined DSN for Go app
DATABASE_URL=postgres://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}?sslmode=disable